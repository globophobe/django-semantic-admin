Contributing
------------

Install dependencies with `uv sync --group dev`. The demo is built with [invoke tasks](https://github.com/globophobe/django-semantic-admin/blob/master/demo/tasks.py). For example, `cd demo; uv run invoke build`.
